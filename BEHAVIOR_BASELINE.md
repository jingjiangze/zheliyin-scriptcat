# BEHAVIOR_BASELINE.md — 行为基线（Golden Master）

> 本文件定义 R0 确认的「不可变业务契约」。此后所有重构的验收标准都以本文件为准。
> R0 阶段仅记录，未运行回归执行；回归执行与记录见 R1（`TEST_PLAN.md` + `TEST_REPORT.md`）。

---

## 1. Golden Master 快照（2026-09-15 抓取）

| 项 | 值 |
|---|---|
| 仓库 | `jingjiangze/zheliyin-scriptcat` |
| 默认分支 | `main` |
| main HEAD | `6c19b4623bd9b96fdc845b0a8d88a9f5b21f32eb`（short `6c19b46`） |
| 唯一 tag | `v0.3.0` @ `d03e014`（可追溯回滚点，已发布 Release） |
| 最新 Release | `v0.3.0`（2026-09-10，含 `ZheliyinCardInstaller.exe`） |
| 主脚本 blob（main） | `79813ba45413ca96d3394fe6bbc2943134081e4d` |
| 本机主脚本 MD5 | `AF9E4B59ECF842E242C508770FBBF854` |
| 主脚本自述版本 | `@version` = `0.3.0.0`，`VERSION` = `0.3.0.0` |
| 最新 CI | `34440907969` completed/success |

结论：仓库已有对应稳定 tag（v0.3.0），按规则**不重复建立** v0.2.3.11-baseline；Golden Master = main HEAD `6c19b46`（与已发布 v0.3.0 遥测一致）。后续任何重构 commit 都以「与 6c19b46 的主脚本行为一致」为回归基准。

## 2. Compatibility Contract（兼容性契约）

以下视为**业务行为**，不是实现习惯。任何阶段不得改变其用户可感知结果：

1. 优先使用模板已有文字图层。
2. 已有文字图层**默认只改文字**，不强制覆盖原字号/位置/样式。
3. 已有图层不足时才创建新文字图层。
4. 新文字图层尽量继承附近文字图层样式（字体/颜色/字号/行高/位置节奏）。
5. 多电话、多地址、微信、邮箱、网址保持多值支持（多行字段）。
6. 界面不出现「电话1、电话2、地址1、地址2」等内部序号名称（统一「电话：…/地址：…」）。
7. 正面/反面字段分区（正面内容区 / 反面内容区）保持。
8. 公司名、姓名、职位、联系方式、地址等优先进入正面。
9. 主营业务、简介、宣传语等优先进入反面。
10. 本地规则优先分类，AI 只辅助补充，**不允许 AI 覆盖本地可靠判断**。
11. AI 不可用（无 Key/网络/超时/报错/非法 JSON）时必须回退本地规则。
12. AI 值必须能在用户原文中命中才可采纳；地址须过 `isAddressLine`、主营须过 `isBusinessLine`。
13. 现有「识别并填正反面 / 追加信息 / 填正面 / 填反面 / 画布自检」按钮功能保持。
14. 不能删除模板原有文字图层。
15. 只能清理助手自己创建且本轮不再需要的自动生成图层。
16. 保留 GitHub 更新检测与版本更新提示（面板内链接形式）。
17. 现有 postMessage + 页面桥接机制继续使用（probe/apply/applyResult/probeResult）。
18. 无 AI Key 时全程本地可运行（纯本地规则完整链路）。

## 3. 正反面规则行为规格（基线版，供回归断言）

| 输入类别 | 行为 |
|---|---|
| 显式标记 | 行首「正面/反面/背面(+内容)」切分后续行 |
| 正面强特征 | 公司行、地址行、带边界电话、邮箱、微信/网址词、职位词、纯中文2-4字（排除主营/反面/排除词） |
| 反面强特征 | 主营行（业务/行业词）、反面补充行（简介/优势/标语/二维码…） |
| 模糊行 | 长段说明（≥18字、含标点或营销词）→ 反面；其余 → 正面 |
| 公司名 | 中文候选中「有限公司/集团」结尾优先 + 剥尾部部门；英文行须含 CO/LTD/LIMITED 等 |
| 姓名 | 纯中文2-4字且非职位/业务/排除词；或「姓名 空格 职位」结构 |
| 职位 | `titleWords` 首命中词 |
| 电话 | `PHONE_BOUND_RE`（前后非数字），贴标签「电话：」 |
| 微信 | 正/反面行均扫，「微信/wechat/wx」前缀剥离 |
| 邮箱/网址 | 正则全扫；网址剔除已并入邮箱的子串 |
| 地址 | ≥6字符、含省市道路等词、非主营 |
| 主营 | 业务/行业词行；多条可并存 |
| 反面补充 | 简介/优势/承诺/二维码/标语类 |
| 追加信息 | 合并保留既有值，去重 |

## 4. 业务行为 vs 实现方式（判定模板）

| 业务行为（不可变） | 当前实现方式（可变） |
|---|---|
| 已有层优先填充 | `pickObject`/`scoreObject` 打分选层 |
| 已有层只改文本 | `setObjectText`（不动字号/位置/样式） |
| 不足才建层 | `createTextObject`（克隆参考样式） |
| 多余助手层清理 | `removeAssistantExtras` + 标签 `zyCreatedByAssistant` |
| 本地规则优先 | `parseByRulesFromSides` → `mergeFields`（AI 仅补空） |
| 正反面分区 | `splitFrontBackText` / `splitSidesByDoubao` → `{front,back}` |
| 桥接填画布 | `window.postMessage` + 页面注入脚本 |

> 未来允许替换任意「实现方式」，不得改变「业务行为」结果。

## 5. 验收标准

每阶段重构通过条件：
1. §2 契约 18 项逐一对照无回归；
2. TEST_PLAN.md 回归矩阵 Case A–T 结果 ≥ 迁移前基线（PASS 集不缩水）；
3. 主脚本 `@version` 与 `VERSION` 一致；更新检测不自我误报；
4. 生成层标签语义不破坏清理与模板保护；
5. 每个 commit 可独立回滚。

## 6. 变更记录

| 日期 | 内容 |
|---|---|
| 2026-09-15 | R0 建立本基线（只记录，未改动源码） |