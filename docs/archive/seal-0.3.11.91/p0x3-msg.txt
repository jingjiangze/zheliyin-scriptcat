test(46h): P0-X 收尾 —— 46h 3/3 用例 3 轮全绿（纯测试口径修复）

根因（由点击后探针数据决定性定位）：
- 探针证实点击已生效（clicked=true, panelMinimized=false, btnVisible=true），且确认键由 confirmVisible=false -> true 正常生成；
- 实际状态文案为「AI 槽位匹配不可用（no-key），已改用本地规则库匹配（预览，未修改画布）：本地规则匹配 3/3 槽」；
- 而驱动只识别写死的 AI 分支文案「AI 槽位匹配完成（预览，未修改画布）」-> 未点确认键 -> 画布未改 -> TEXT@1 保持模板值、status 读取为空。

修复（纯 runner，不改产品代码、不动脚本版本）：
- 预览识别改为通配「（预览，未修改画布）」，同时覆盖 AI 分支与本地规则库降级分支；
- 终止/完成文案集合扩充（更新 N 槽 / 未找到匹配 / 无需匹配 等），避免空等。

真机结果（0.3.11.91，3 轮）：
- C-MAIN  errors=[] x3  -> 「AI 填充完成：更新 3 槽（几何/字体/样式/身份/层序冻结，只改文字）」；
- C-NO_MATCH / C-EMPTY 保持绿；
- 即 46h 3/3 用例、3 轮全绿；结论：该失败自始为过期验收口径（测试侧正则写死单分支），非产品回归 —— 产品代码零改动。
- 回归 57/58（唯一失败为既有无关项 stage8d/text-fit-fusion）。