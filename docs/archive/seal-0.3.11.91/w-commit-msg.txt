ui(panel): 去掉诊断 UI + 追加信息改为识别图片文字 + 复制行改 [复制正反面][复制全部文字] @0.3.11.88

用户指令（2026-09-24）三处 UI 精简：

- (1) 去掉诊断 UI：移除 #zy-probe「诊断」按钮及其绑定（probeCanvas 内部函数保留，供 __ZY_DEBUG__ 用）。
- (2) 「追加信息」原地改为「识别图片文字」：该按钮 id 改为 zy-ocr-btn（原 #zy-append 绑定移除）；
      同时删除下方重复的 [识别图片文字][诊断] 整行 —— 顶部动作区变为 [一键智能填充][识别图片文字]。
- (3) 复制行改为两键：[复制正反面][复制全部文字]，去掉「复制当前面」与「复制套版结构」；
      新增 zyBuildCopyText 的 all 模式（正反面全部文字，纯文本逐行、不带标题与结构字段），
      复制状态标签新增「全部文字」。template 结构化分支保留（能力不删，仅撤 UI 入口）。

同步更新真机 runner 断言：
- commit-46o：三模式（current/both/template）改为两模式（both/all），并新增负断言
  「不得含【正面】/【反面】标题、不得含 字号= / 位置=( 结构字段」。
- commit-46p：copy-btns 断言改为新两键存在且旧两键（zy-copy-current/zy-copy-template）必须缺席。

真机验收 commit-46o（0.3.11.88）errors=[]：
- 复制正反面 → 「已复制正反面 6 行到剪贴板。」
- 复制全部文字 → 「已复制全部文字 5 行到剪贴板。」（纯文本逐行；无标题、无结构字段）
- 画布零变化（canvasFails=[]）

selftest：46o html-2btns / 46p copy-btns 全绿；本地回归 56/57（唯一失败为既有无关项 stage8d/text-fit-fusion）。
版本 bump 0.3.11.87 -> 0.3.11.88（RULE-18）；@require 50 条不变。
