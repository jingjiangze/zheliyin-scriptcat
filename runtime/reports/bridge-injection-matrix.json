{
  "stage": "RUNTIME-8",
  "ts_init": "2026-09-15",
  "ts_final": "2026-09-16",
  "scope": "Bridge Injection Compatibility Audit（只研究 isolated→page world 注入路径；不禁改 Bridge protocol 与生产代码）",
  "status": "COMPLETE_REAL_SCRIPT_CAT_FULL_PASS",
  "matrix": {
    "dom-inline-script": {
      "status": "FAIL_CSP_BLOCKED",
      "evidence": [
        "真实编辑器页 Console: Executing inline script violates Content Security Policy (script-src 无 unsafe-inline)",
        "installPageBridge 的 <script>.textContent 注入后 marker __ZY_CARD_ASSISTANT_BRIDGE__ 恒为 null",
        "同页 page.evaluate（调试通道绕过 CSP）注入 pageBridge 后 marker/probe/防重全部 PASS —— 证明协议本身正常、仅传输载体被拦"
      ],
      "cts": "assistant.js:861-878 / userscript.js:861-878（同一实现）"
    },
    "scriptcat-gm-add-element": {
      "status": "REAL_PASS（真实 ScriptCat userscript 内 GM_addElement 注入 production pageBridge 全链闭环）",
      "evidence": [
        "RUNTIME-8.3 解锁 chrome.userScripts（根因：Chrome 138+ 需扩展详情页 Allow User Scripts 开关 → 自动开启 + reload 后 API 可用，分类 A:API_SUPPORTED_AND_ENABLED；RUNTIME-8.2 的『Chromium 不支持』结论撤回）",
        "真实 ScriptCat 注册两个 userscript（installByCode status=1 → getAllScripts 可见）：min-exec（DOM 落 data-zy-runtime8-user-script=1，REAL_USER_SCRIPT_EXECUTION）+ GM probe（@grant GM_addElement）",
        "GM_addElement('script',{textContent: production pageBridge 源码}) → window.__ZY_CARD_ASSISTANT_BRIDGE__.installed=true（marker，ts 与注入时序一致）；probe 1 请求→1 响应、5 请求→5 响应；跨实例重复安装 ×3 仍恰 1 响应（verify-gm-bridge）",
        "Apply：postMessage apply side=front name=TEST_RUNTIME_VALUE → applyResult ok:true『正面 姓名 -> TEST_RUNTIME_VALUE』→ 画布 readback 命中 → snapshot-diff rollback restored=1 leaked=0 total 21=21（verify-gm-apply / runtime8-full-chain-report）",
        "响应来源核验：source=zy-card-assistant-page 唯一 listener；注入源码 sha256:17141f7c67fb4862（page-bridge.js）；全程无 DUP_SIM（§九 禁止 page.evaluate(pageBridge)/crossInstanceDuplicate）"
      ],
      "note": "环境前提记录：需支持 chrome.userScripts 的载体（Chrome 120+/Playwright Chromium 153 + userScripts 权限 + Allow User Scripts 开关），关闭该开关时 API 表现为 undefined 需区分 UNSUPPORTED/DISABLED/POLICY（§十六）"
    },
    "extension-main-world": {
      "status": "PASS（MAIN world 通道等价证明）",
      "evidence": [
        "真实编辑器（REAL_LOGGED_IN_EDITOR）：page.evaluate 主世界注入 pageBridge（语义等价 chrome.scripting.executeScript({world:'MAIN'})）",
        "干净流程（无 DUP_SIM）：注入前 marker=null → 注入后 marker.installed=true → 1 probe 恰 1 响应 → 5 probe 恰 5 响应 → 真实 Canvas 619.5×376.86/21 对象",
        "证明：MAIN world 注入 → Bridge 全链路在真实页面成立，且与页面 CSP 无关"
      ],
      "note": "这是主世界通道的等价证明（调试通道），非「真实扩展 background executeScript」的二进制证据；production 采用需真实 executeScript 复验（§20/§21 如实区分）"
    },
    "real-scriptcat": {
      "status": "REAL_FULL_CHAIN_PASS（npm run runtime:scriptcat 18 步 errors=0）",
      "evidence": [
        "真实 ScriptCat 扩展在 Playwright 中加载、SW 运行、options 渲染（REAL_SCRIPT_CAT_EXTENSION）",
        "脚本自动安装全部真实通过（REAL_SCRIPT_CAT_INSTALL）：installByCode 返回完整 script 对象（status=1/grant/match 解析）→ getAllScripts 可查 → deletes 可删",
        "RUNTIME-8.3 解锁注入：Allow User Scripts 自动开启（chrome://extensions toggle row allow-user-scripts）+ 扩展 reload 后 chrome.userScripts 可用 → registerUserscripts() isUserScriptsAvailable=true → min-exec userScript 真实执行（DOM mark）",
        "全链闭环（runtime8-full-chain-report.json, errors=0）：install min-exec+GM → 真实执行 → GM_addElement(production pageBridge) → marker → probe exactly-one/5x-5 → Canvas 619.5×376.86/21 对象 → apply ok:true → readback 命中 → rollback 21=21 → cleanup",
        "RUNTIME-8.2 曾因 harness 无 userScripts API 判定 INJECTION_BLOCKED —— 根因实为 Allow User Scripts 开关（Chrome 138+ 新增要求），非 Chromium 构建缺陷，已撤回原结论"
      ],
      "note": "结论由 BLOCKED 修正为 PASS（§一 用户纠正验证价值：undefined 需与 getScripts() throw / policy 区分，见 §十六 A/B/C/D）"
    },
    "real-extension": {
      "status": "MAIN_WORLD_PASS_EQUIV（无 DUP_SIM 污染）",
      "evidence": [
        "本次 RUNTIME-8 探针以干净流程完成（注入前 marker=null），排除任何测试注入污染",
        "真实面板渲染（content script 层）+ 真实 Canvas 619.5×376.86/21 对象"
      ],
      "note": "extension 侧污染路径（DUP_SIM 后的 bridge-apply）已按 §14/§38 修正为 BRIDGE_APPLY_WITH_SIMULATED_INSTALLATION"
    }
  },
  "correction_applied": {
    "issue": "§14 full.js 中 bridge-apply 在 crossInstanceDuplicate(DUP_SIM) 之后执行，原证据等级 REAL_BRIDGE+REAL_APPLY 不成立",
    "fix": "已改为 BRIDGE_APPLY_WITH_SIMULATED_INSTALLATION；真实载体 Apply 需注入方案验证通过后另行完成",
    "file": "runtime/full.js bridge-apply rec evidence"
  },
  "final_matrix_table": [
    { "injection": "DOM inline", "browser": "REAL", "carrier": "Userscript", "page world": "MAIN", "csp": "blocked", "result": "FAIL/BLOCKED（真实 CSP violation）" },
    { "injection": "GM_addElement", "browser": "REAL(vendor ScriptCat v1.4.0)", "carrier": "ScriptCat real userscript", "page world": "MAIN", "csp": "bypass", "result": "REAL PASS（marker/probe/apply/rollback 全链闭环，Allow User Scripts 解锁后）" },
    { "injection": "executeScript MAIN(等价值)", "browser": "REAL", "carrier": "MV3(通道证明)", "page world": "MAIN", "csp": "bypass", "result": "PASS(clean)" }
  ],
  "decisions": {
    "rewrite_bridge": "NO",
    "change_protocol": "NO",
    "production_change": "CONFIRMED — GM_addElement 真实可绕过折立印 CSP 注入 production pageBridge（RUNTIME-8.3 全链 REAL PASS）；RUNTIME-8.2 的『Chromium 不支持 userScripts』结论撤销（根因 Allow User Scripts 开关，Chrome 138+ 要求）。production 注入迁移（ScriptCat GM_addElement / MV3 executeScript MAIN）进入后续阶段按证据落地"
  },
  "gate": "REAL_SCRIPT_CAT_FULL = PASS"
}