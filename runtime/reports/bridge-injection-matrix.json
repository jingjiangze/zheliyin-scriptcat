{
  "stage": "RUNTIME-8",
  "ts_init": "2026-09-15",
  "ts_final": "2026-09-15",
  "scope": "Bridge Injection Compatibility Audit（只研究 isolated→page world 注入路径；不禁改 Bridge protocol 与生产代码）",
  "status": "COMPLETE_CONDITIONAL",
  "matrix": {
    "dom-inline-script": {
      "status": "FAIL_CSP_BLOCKED",
      "evidence": [
        "真实编辑器页 Console: Executing inline script violates Content Security Policy (script-src 无 unsafe-inline)",
        "installPageBridge 的 <script>.textContent 注入后 marker __ZY_CARD_ASSISTANT_BRIDGE__ 恒为 null",
        "同页 page.evaluate（调试通道绕过 CSP）注入 pageBridge 后 marker/probe/防重全部 PASS —— 证明协议本身正常、仅传输载体被拦"
      ],
      "cts": "assistant.js:861-878 / userscript.js:861-878（同一实现）"
    },
    "scriptcat-gm-add-element": {
      "status": "BLOCKED_AUTOMATION（有官方声明，缺真实运行实证）",
      "evidence": [
        "ScriptCat 官方 API 文档: GM_addElement(tag, attrs) 明示「可以绕过 CSP 限制」",
        "ScriptCat 扩展本体实装 GM_addElement（vendor 源码 chunk.js:194 类型声明 + 多处实现）；manifest 含 scripting/userScripts 权限",
        "Playwright 已能真实加载 vendor ScriptCat（chrome-extension://ndcooeabab… service_worker 运行、options 页渲染）",
        "自动安装 userscript 受限于：① Monaco 编辑器 UI 自动化不稳定（聚焦成功但模型写入不落地）② install.html?url= 返回无效页面 ③ installByCode 走 service worker 内部消息（不猜 schema，未伪造写入）"
      ],
      "note": "GM_addElement 为官方文档声明的 CSP 绕过通道，但依据 §27「只有证据充分才 PASS」——未取得真实运行证据前保持 BLOCKED，不写 PASS"
    },
    "extension-main-world": {
      "status": "PASS（MAIN world 通道等价证明）",
      "evidence": [
        "真实编辑器（REAL_LOGGED_IN_EDITOR）：page.evaluate 主世界注入 pageBridge（语义等价 chrome.scripting.executeScript({world:'MAIN'})）",
        "干净流程（无 DUP_SIM）：注入前 marker=null → 注入后 marker.installed=true → 1 probe 恰 1 响应 → 5 probe 恰 5 响应 → 真实 Canvas 619.5×376.86/21 对象",
        "证明：MAIN world 注入 → Bridge 全链路在真实页面成立，且与页面 CSP 无关"
      ],
      "note": "这是主世界通道的等价证明（调试通道），非「真实扩展 background executeScript」的二进制证据；production 采用需真实 executeScript 复验（§20/§21 如实区分）"
    },
    "real-scriptcat": {
      "status": "BLOCKED（dedicated 自动化受限）",
      "evidence": [
        "vendored ScriptCat 可加载、可渲染管理页，但自动安装 userscript 的三条候选路径（编辑器 UI / install.html?url / 内部 installByCode）均未稳定打通",
        "installByCode 走 service worker 内部消息流，不暴露调用面；未伪造 storage 写入（§8/§28）"
      ],
      "note": "当前不构成 REAL_SCRIPT_CAT_BRIDGE=PASS；如需升级需用户真实 ScriptCat 会话或后续可靠安装路径（不在本阶段伪造）"
    },
    "real-extension": {
      "status": "MAIN_WORLD_PASS_EQUIV（无 DUP_SIM 污染）",
      "evidence": [
        "本次 RUNTIME-8 探针以干净流程完成（注入前 marker=null），排除任何测试注入污染",
        "真实面板渲染（content script 层）+ 真实 Canvas 619.5×376.86/21 对象"
      ],
      "note": "extension 侧污染路径（DUP_SIM 后的 bridge-apply）已按 §14/§38 修正为 BRIDGE_APPLY_WITH_SIMULATED_INSTALLATION"
    }
  },
  "correction_applied": {
    "issue": "§14 full.js 中 bridge-apply 在 crossInstanceDuplicate(DUP_SIM) 之后执行，原证据等级 REAL_BRIDGE+REAL_APPLY 不成立",
    "fix": "已改为 BRIDGE_APPLY_WITH_SIMULATED_INSTALLATION；真实载体 Apply 需注入方案验证通过后另行完成",
    "file": "runtime/full.js bridge-apply rec evidence"
  },
  "final_matrix_table": [
    { "injection": "DOM inline", "browser": "REAL", "carrier": "Userscript", "page world": "MAIN", "csp": "blocked", "result": "FAIL/BLOCKED" },
    { "injection": "GM_addElement", "browser": "REAL(ScriptCat vendored)", "carrier": "ScriptCat", "page world": "MAIN", "csp": "bypass(官方声明)", "result": "BLOCKED(未实测 PASS)" },
    { "injection": "executeScript MAIN(等价值)", "browser": "REAL", "carrier": "MV3(通道证明)", "page world": "MAIN", "csp": "bypass", "result": "PASS(clean)" }
  ],
  "decisions": {
    "rewrite_bridge": "NO",
    "change_protocol": "NO",
    "production_change": "PENDING — MAIN world 通道已证可行；但 production 采用 executeScript/GM_addElement 需真实载体验证后再定（§39-41）。当前表现状（DOM inline）在折立印页面 CSP 下 Bridge 不可安装，属真实发现项，需在后续阶段按证据最小修复"
  },
  "gate": "CONDITIONAL-GO"
}